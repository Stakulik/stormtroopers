%p Hello #{@user.email}

%p Someone has requested a link to change your password. You can do this through the link below.

%p= link_to 'Change my password', "http://localhost:8080/reset_password?reset_password_token=#{@user.reset_password_token}"

%p= link_to 'Change my password', "http://starwars.wearevolt.com/reset_password?reset_password_token=#{@user.reset_password_token}"

%p You must confirm reset password for 2 hours.

%p If you didn't request this, please ignore this email.

%p Your password won't change until you access the link above and create a new one.
